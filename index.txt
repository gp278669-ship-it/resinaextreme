<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resina Extreme - Revitalização de Pintura e Plásticos</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #0a0a0a;
            color: #fff;
        }

        /* HEADER */
        header {
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            text-align: center;
            padding: 15px;
            border-bottom: 3px solid #e63946;
        }

        header p {
            font-size: 14px;
            color: #f1c40f;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        /* HERO */
        .hero {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #0a0a0a 100%);
            text-align: center;
            padding: 60px 20px;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: radial-gradient(ellipse at center, rgba(230,57,70,0.15) 0%, transparent 70%);
        }

        .hero-badge {
            display: inline-block;
            background: #e63946;
            color: #fff;
            padding: 8px 20px;
            border-radius: 50px;
            font-size: 13px;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-bottom: 25px;
        }

        .hero h1 {
            font-size: clamp(28px, 5vw, 52px);
            font-weight: 900;
            line-height: 1.2;
            margin-bottom: 20px;
            text-transform: uppercase;
        }

        .hero h1 span {
            color: #e63946;
        }

        .hero p {
            font-size: clamp(16px, 2.5vw, 20px);
            color: #ccc;
            max-width: 650px;
            margin: 0 auto 35px;
            line-height: 1.7;
        }

        .hero-img {
            width: 100%;
            max-width: 500px;
            margin: 30px auto;
            display: block;
            border-radius: 15px;
            border: 3px solid #e63946;
            box-shadow: 0 0 40px rgba(230,57,70,0.4);
        }

        /* PAGAMENTO NA ENTREGA DESTAQUE */
        .entrega-banner {
            background: linear-gradient(135deg, #f1c40f, #f39c12);
            color: #000;
            text-align: center;
            padding: 20px;
            font-size: clamp(16px, 3vw, 22px);
            font-weight: 900;
            letter-spacing: 1px;
        }

        .entrega-banner span {
            font-size: clamp(20px, 4vw, 28px);
        }

        /* SECTIONS */
        section {
            padding: 60px 20px;
            max-width: 900px;
            margin: 0 auto;
        }

        .section-title {
            text-align: center;
            font-size: clamp(22px, 4vw, 36px);
            font-weight: 900;
            text-transform: uppercase;
            margin-bottom: 40px;
        }

        .section-title span {
            color: #e63946;
        }

        /* BENEFÍCIOS */
        .benefits-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .benefit-card {
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            border: 1px solid #e63946;
            border-radius: 12px;
            padding: 25px;
            text-align: center;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .benefit-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(230,57,70,0.3);
        }

        .benefit-card .icon {
            font-size: 40px;
            margin-bottom: 15px;
        }

        .benefit-card h3 {
            font-size: 18px;
            margin-bottom: 10px;
            color: #f1c40f;
        }

        .benefit-card p {
            font-size: 14px;
            color: #aaa;
            line-height: 1.6;
        }

        /* COMO USAR */
        .steps {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .step {
            display: flex;
            align-items: center;
            gap: 20px;
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            border-radius: 12px;
            padding: 20px 25px;
            border-left: 5px solid #e63946;
        }

        .step-number {
            background: #e63946;
            color: #fff;
            width: 45px;
            height: 45px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            font-weight: 900;
            flex-shrink: 0;
        }

        .step-text h3 {
            font-size: 18px;
            margin-bottom: 5px;
            color: #f1c40f;
        }

        .step-text p {
            font-size: 14px;
            color: #aaa;
        }

        /* DIFERENCIAIS PAGAMENTO */
        .payment-section {
            background: linear-gradient(135deg, #1a1a2e, #0a0a0a);
            border-radius: 20px;
            padding: 50px 30px;
            text-align: center;
            border: 2px solid #f1c40f;
            box-shadow: 0 0 40px rgba(241,196,15,0.2);
            margin: 0 20px;
        }

        .payment-section .section-title {
            color: #f1c40f;
        }

        .payment-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .payment-card {
            background: rgba(241,196,15,0.1);
            border: 1px solid #f1c40f;
            border-radius: 12px;
            padding: 25px 15px;
            text-align: center;
        }

        .payment-card .icon {
            font-size: 40px;
            margin-bottom: 12px;
        }

        .payment-card h3 {
            font-size: 16px;
            color: #f1c40f;
            margin-bottom: 8px;
        }

        .payment-card p {
            font-size: 13px;
            color: #ccc;
            line-height: 1.5;
        }

        /* DEPOIMENTOS */
        .testimonials {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 20px;
        }

        .testimonial {
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            border-radius: 12px;
            padding: 25px;
            border-top: 3px solid #e63946;
        }

        .stars {
            color: #f1c40f;
            font-size: 20px;
            margin-bottom: 12px;
        }

        .testimonial p {
            font-size: 14px;
            color: #ccc;
            line-height: 1.7;
            font-style: italic;
            margin-bottom: 15px;
        }

        .testimonial .author {
            font-weight: 700;
            color: #f1c40f;
            font-size: 14px;
        }

        /* OFERTA */
        .offer-box {
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            border: 3px solid #e63946;
            border-radius: 20px;
            padding: 50px 30px;
            text-align: center;
            box-shadow: 0 0 60px rgba(230,57,70,0.3);
            margin: 0 20px;
        }

        .offer-box .timer {
            background: #e63946;
            display: inline-block;
            padding: 8px 25px;
            border-radius: 50px;
            font-size: 13px;
            font-weight: 700;
            letter-spacing: 2px;
            margin-bottom: 25px;
        }

        .price-old {
            font-size: 20px;
            color: #888;
            text-decoration: line-through;
            margin-bottom: 5px;
        }

        .price-new {
            font-size: clamp(42px, 8vw, 72px);
            font-weight: 900;
            color: #f1c40f;
            line-height: 1;
            margin-bottom: 10px;
        }

        .price-new span {
            font-size: 30px;
        }

        .price-info {
            font-size: 16px;
            color: #ccc;
            margin-bottom: 30px;
        }

        .cta-btn {
            display: inline-block;
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: #fff;
            padding: 20px 50px;
            border-radius: 50px;
            font-size: clamp(18px, 3vw, 24px);
            font-weight: 900;
            text-decoration: none;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 8px 30px rgba(46,204,113,0.4);
            transition: transform 0.2s, box-shadow 0.2s;
            cursor: pointer;
            border: none;
            animation: pulse 2s infinite;
        }

        .cta-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 12px 40px rgba(46,204,113,0.6);
        }

        @keyframes pulse {
            0%, 100% { box-shadow: 0 8px 30px rgba(46,204,113,0.4); }
            50% { box-shadow: 0 8px 50px rgba(46,204,113,0.8); }
        }

        .offer-extras {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 15px;
            margin-top: 30px;
        }

        .offer-extra {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 14px;
            color: #ccc;
        }

        .offer-extra span {
            color: #2ecc71;
            font-size: 18px;
        }

        /* GARANTIA */
        .guarantee {
            background: linear-gradient(135deg, #1a3a1a, #0a2a0a);
            border: 2px solid #2ecc71;
            border-radius: 20px;
            padding: 40px 30px;
            text-align: center;
            margin: 0 20px;
        }

        .guarantee .icon {
            font-size: 70px;
            margin-bottom: 15px;
        }

        .guarantee h2 {
            font-size: clamp(20px, 4vw, 30px);
            color: #2ecc71;
            margin-bottom: 15px;
        }

        .guarantee p {
            font-size: 16px;
            color: #ccc;
            line-height: 1.7;
            max-width: 600px;
            margin: 0 auto;
        }

        /* FAQ */
        .faq-list {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .faq-item {
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            border-radius: 12px;
            padding: 20px 25px;
            border-left: 4px solid #f1c40f;
        }

        .faq-item h3 {
            font-size: 16px;
            color: #f1c40f;
            margin-bottom: 8px;
        }

        .faq-item p {
            font-size: 14px;
            color: #aaa;
            line-height: 1.6;
        }

        /* FOOTER */
        footer {
            background: #050505;
            text-align: center;
            padding: 30px 20px;
            color: #555;
            font-size: 13px;
            border-top: 1px solid #1a1a2e;
        }

        /* COUNTDOWN */
        .countdown {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin: 20px 0;
        }

        .countdown-item {
            background: #e63946;
            border-radius: 10px;
            padding: 10px 15px;
            text-align: center;
            min-width: 65px;
        }

        .countdown-item .num {
            font-size: 28px;
            font-weight: 900;
            display: block;
        }

        .countdown-item .label {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* RESPONSIVE */
        @media (max-width: 600px) {
            .step {
                flex-direction: column;
                text-align: center;
            }
            .offer-extras {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</head>
<body>

    <!-- HEADER -->
    <header>
        <p>🚚 Pagamento na Entrega Disponível — Sem Risco para Você!</p>
    </header>

    <!-- HERO -->
    <div class="hero">
        <div class="hero-badge">🔥 Novidade no Mercado</div>
        <h1>Devolva a Vida à <span>Pintura do Seu Carro</span> em Minutos!</h1>
        <p
